---
title: "51 io.Reader/io.Writer 与资源所有权"
tags:
  - nginx-gateway-fabric
  - go-1-26
  - source-analysis
  - tutorial
status: complete
note_type: tutorial
go_version: "1.26.0"
repo_revision: "df175d68064b54c369b6cb2a6c83c8c1b2ab26ca"
sources:
  - repo: nginx-gateway-fabric
    revision: "df175d68064b54c369b6cb2a6c83c8c1b2ab26ca"
    dirty: false
---

# 51 io.Reader/io.Writer 与资源所有权

> [!abstract] 本章唯一知识点
> Reader/Writer 以流能力解耦数据源和目的地；接口不自动定义 Close，资源所有权必须另行约定。

## 前置与完成标准

前置：[[50-slices-maps与cmp]]。学完应能解释“io.Reader/io.Writer 与资源所有权”，并在 NGF 中定位 `StdLibOSFileManager.Copy`，说清调用效果与适用边界。本章只做源码导读，片段不承诺可独立运行。

## 最小模型

阅读时先判断类型、所有权和生命周期由谁规定，再读语法；这样能把记忆中的写法重新连回工程约束。

## NGF 生产代码证据

**internal/framework/file/os_filemanager.go · StdLibOSFileManager.Copy（原样摘录）**

```go
func (s *StdLibOSFileManager) Copy(dst io.Writer, src io.Reader) error {
```

- 定义：`ngf:internal/framework/file/os_filemanager.go:StdLibOSFileManager.Copy`
- 精简链路：调用者打开源/目标并拥有 Close → Copy 只传输字节 → 返回写入错误。
- 测试佐证：`internal/framework/file/file_test.go`
- 证据范围：源码事实固定于 `df175d68`；测试文件用于确认行为边界，不把框架推断写成项目事实。

## 工程取舍与边界

小接口便于替换文件系统并测试错误分支；打开者负责关闭。

> [!warning] 常见误解与迁移边界
> 可直接复用 Reader/Writer 组合；处理不可信输入时限制大小。误解是 io.Copy 会关闭两端。

复用判定：明确可复用的做法可直接采用；带条件的做法必须先满足相同输入、所有权与失败语义；指出不要或不可的部分不应照搬。

## 心智模型与下一步

一句话心智模型：**io.Reader/io.Writer 与资源所有权不是孤立语法，而是 `StdLibOSFileManager.Copy` 所在边界用来表达约束的工具。**

上一章：[[50-slices-maps与cmp]] · 下一章：[[52-encoding-json与模型转换]]

延伸阅读：[io package](https://pkg.go.dev/io)
